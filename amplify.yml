version: 1
backend:
  phases:
    build:
      commands:
        - npm install # Installation des dépendances Node.js
        - npm run start # ou une autre commande pour démarrer votre serveur
artifacts:
  baseDirectory: /
  files:
    - '**/*'
cache:
  paths:
    - node_modules/**/*
