version: 1
backend:
  phases:
    build:
      commands:
        - npm install  # Installation des dépendances Node.js
        - npm start    # Commande pour démarrer votre application backend, qui doit être définie dans votre package.json
  # Pas d'artifacts nécessaire pour une API
  # artifacts:
  #   baseDirectory: /
  #   files:
  #     - '**/*'
  cache:
    paths:
      - node_modules/**/*
